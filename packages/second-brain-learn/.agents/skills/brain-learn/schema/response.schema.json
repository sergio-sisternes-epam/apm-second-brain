{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "second-brain.learn.v1.response",
  "title": "Learn Receipt",
  "description": "Receipt envelope returned by the second-brain.learn.v1 capability.",
  "type": "object",
  "required": ["correlation_id", "source_id", "status"],
  "additionalProperties": false,
  "properties": {
    "correlation_id": {
      "type": "string",
      "format": "uuid",
      "description": "Echoes the correlation_id from the originating request."
    },
    "source_id": {
      "type": "string",
      "description": "Provider-assigned identifier for the ingested source. Use this as target_id in a future brain-forget request."
    },
    "status": {
      "type": "string",
      "enum": ["accepted", "duplicate", "invalid"],
      "description": "Ingestion outcome."
    },
    "message": {
      "type": "string",
      "description": "Human-readable status detail."
    }
  }
}
