{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "second-brain.error",
  "title": "Standard Error Envelope",
  "description": "Error envelope returned by any second-brain capability when the adapter or provider encounters a failure.",
  "type": "object",
  "required": ["correlation_id", "code", "message"],
  "additionalProperties": false,
  "properties": {
    "correlation_id": {
      "type": "string",
      "format": "uuid",
      "description": "Echoes the correlation_id from the originating request, or a provider-generated UUID if the request was malformed."
    },
    "code": {
      "type": "string",
      "enum": [
        "VALIDATION_ERROR",
        "NOT_FOUND",
        "UNAUTHORIZED",
        "PROVIDER_ERROR",
        "TRANSIENT",
        "UNKNOWN"
      ],
      "description": "Machine-readable error code. TRANSIENT indicates a safe retry."
    },
    "message": {
      "type": "string",
      "description": "Human-readable description of the error."
    },
    "detail": {
      "type": "object",
      "description": "Optional structured detail for programmatic handling.",
      "additionalProperties": true
    }
  }
}
