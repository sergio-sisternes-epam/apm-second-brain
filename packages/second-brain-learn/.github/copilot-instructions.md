<!-- Generated by APM CLI from distributed .apm/ primitives -->
<!-- Do not edit manually. To regenerate: apm compile -->

# second-brain-learn -- Provider Context

This package provides the internal write and validation layer for the second
brain system. All skills are internal -- they are invoked by higher-level
client skills (brain-learn, brain-forget) and must not be called directly by users.

## Internal skills

- **sb-learn-handler** -- Receives a validated learn request envelope, deduplicates
  by content hash, writes to the wiki via kw-wiki-ingest, and returns a learn
  receipt (status: accepted | duplicate | invalid).

- **sb-forget-handler** -- Receives a validated forget request envelope, tombstones
  the target concept via kw-wiki-archive (v1 is tombstone-only -- no destructive
  deletion), and returns a forget receipt (status: tombstoned | not_found).

- **sb-learn-validate** -- Validates a learn request envelope against the
  second-brain-interfaces schema before any wiki write. Returns validation
  result (valid: true/false, errors: [...]).

## Invocation model

All skills have `direct-user-invocation: disabled`. Callers must use the
brain-learn and brain-forget client skills from second-brain-interfaces.
